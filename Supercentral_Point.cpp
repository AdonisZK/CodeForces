#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    int query, x, y, ans = 0;
    cin >> query;

    for (int i = 0; i < query; i++)
    {
        cin >> x >> y;
    }
    return 0;
}

