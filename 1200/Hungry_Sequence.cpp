#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int n, count = 0, i = 5000000;
    cin >> n;
    while (count != n)
    {
        cout << i << endl;
        i++;
        count++;
    }
    return 0;
}