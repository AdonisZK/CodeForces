#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int n, i, temp, a, b, c;
    vector<int> arr;
    vector<int>::iterator it;
    cin >> n;
    for (i = 0; i < n; i++)
    {
        cin >> temp;
        arr.push_back(temp);
    }
    it = arr.begin();
    for (i = 0; i < n / 3; i++)
    {
        for (auto it = arr.begin(); it != arr.end(); it++)
        {
            
        }
    }

    return 0;
}