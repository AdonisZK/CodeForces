#define MOD 1000000007

#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int n, m, input, w, h;
    vector<int> v;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> input;
        v.push_back(input);
    }
    cin >> m;
    for (int i = 0; i < m; i++)
    {
        cin >> w >> h;
    }
    return 0;
}